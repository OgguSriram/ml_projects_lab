## 📦 Trained Model

The trained ResNet50 model file (~225 MB) is not included in this repository due to GitHub file size limitations.

### To reproduce the model:
- Run the training script:
```bash
python src/step8_resnet50_finetune.py
